//! Schema drift-guard tests.
//!
//! Wiring only: `common` holds shared fixtures; `coverage` and `serde_keys`
//! hold the test functions grouped by concern.

mod common;
mod coverage;
mod serde_keys;
